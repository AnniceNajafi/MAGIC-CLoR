% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcv.R
\name{magic_tolerant}
\alias{magic_tolerant}
\title{MAGIC with tolerance-regularised MCV t-selection.}
\usage{
magic_tolerant(
  X,
  t_values = 0:10,
  tolerance = 0.05,
  npca = 20L,
  k = 30L,
  ka = 5L,
  split_p = 0.5,
  seed = 1L
)
}
\arguments{
\item{X}{Integer count matrix, cells x genes.}

\item{t_values}{Candidate diffusion times (default `0:10`).}

\item{tolerance}{Fractional tolerance above the MCV minimum (default
`0.05`). Set to `0` to recover the vanilla `mcv_select_t` argmin.}

\item{npca, k, ka}{Graph parameters passed to `magic_graph`.}

\item{split_p}{Train-half mass for Poisson thinning.}

\item{seed}{RNG seed for reproducibility.}
}
\value{
List with `imputed` (cells x genes), `t`, `t_argmin`,
  `tolerance`, `loss`, `graph`.
}
\description{
Picks `t` with [mcv_select_t_tolerant()], then runs [magic_impute()] with
that t on a graph rebuilt from the full data. This is the
tolerant MAGIC workflow we use in the tolerance benchmark.
}
