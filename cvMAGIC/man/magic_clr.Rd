% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clr.R
\name{magic_clr}
\alias{magic_clr}
\title{Run MAGIC and score the gene pairs with the CLR readout}
\usage{
magic_clr(
  X,
  t = 3L,
  npca = 20L,
  k = 30L,
  ka = NULL,
  decay = 1,
  method = "spearman",
  graph = NULL
)
}
\arguments{
\item{X}{integer count matrix, cells x genes (raw UMI counts)}

\item{t}{diffusion time, t = 0 skips imputation and scores the raw counts}

\item{npca, k, ka, decay}{graph parameters passed to magic_graph}

\item{method}{correlation method passed to stats::cor (default "spearman")}

\item{graph}{optional precomputed graph from magic_graph}
}
\value{
a list with imputed (cells x genes), cor (raw correlation), assoc
  (the CLR association) and t
}
\description{
Diffuses the data for t steps (set t = 0 to skip the imputation and just
score the raw counts), takes the gene-gene correlation of the result and
returns the raw correlation together with its CLR version. In our tests the
CLR score picks out TF-target pairs better than the plain correlation, so
that is the score we use for ranking gene pairs.
}
\seealso{
clr_from_cor
}
