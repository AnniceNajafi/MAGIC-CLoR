% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute.R
\name{magic_impute}
\alias{magic_impute}
\title{Run MAGIC imputation with a fixed diffusion time.}
\usage{
magic_impute(X, t = 3L, graph = NULL, ...)
}
\arguments{
\item{X}{Cells x genes data matrix (library-size normalised + sqrt-transformed
is recommended). Must be the same matrix used for graph construction OR a
compatible matrix of identical row count.}

\item{t}{Diffusion time (non-negative integer; default 3).}

\item{graph}{Optional pre-computed graph from `magic_graph()`. If NULL, one is
built from X.}

\item{...}{Passed to `magic_graph()` if `graph` is NULL.}
}
\value{
Imputed cells x genes matrix.
}
\description{
Run MAGIC imputation with a fixed diffusion time.
}
