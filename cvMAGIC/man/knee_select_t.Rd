% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/knee.R
\name{knee_select_t}
\alias{knee_select_t}
\title{Knee-point selection of MAGIC's diffusion time (van Dijk et al., Fig S1C).}
\usage{
knee_select_t(X, t_max = 12L, tol = 0.05, npca = 20L, k = 30L, ka = 5L)
}
\arguments{
\item{X}{cells x genes data matrix (pre-transformed, e.g. sqrt(counts)).}

\item{t_max}{Maximum diffusion time to consider.}

\item{tol}{Stop when relative change drops below this value.}

\item{npca, k, ka}{Graph parameters.}
}
\value{
List with `t` (chosen), `delta` (vector of relative changes), and
  `graph`.
}
\description{
Implements the original heuristic from the MAGIC paper: compute the
Procrustes / Frobenius change between successive imputations,
delta(t) = || M^t X - M^(t-1) X ||_F / || M^(t-1) X ||_F, and pick the
t at which delta(t) drops below a small fractional threshold (the "knee").
This is the reference baseline that cvMAGIC's MCV-based selection replaces.
}
